=== Session init | 2026-02-15 21:55:00 ===
Feature: 项目初始化
Status: completed
Notes: 搭建了 Next.js 16 + TypeScript + Tailwind CSS 项目骨架。创建了核心目录结构（engine/, data/, components/, lib/, services/, types/）。配置了 Jest 测试框架。生成了 feature_list.json（14个功能，按优先级排列）、init.sh、claude-progress.txt。创建了首页、卡组页、对战页的占位页面和导航栏。

=== Session 1 | 2026-02-15 22:10:00 ===
Feature: Pokemon TCG 静态数据集成
Status: completed
Notes: 从 pokemon-tcg-data 下载了 SV 系列 16 个扩展包共 3250 张卡牌数据。创建了完整的 TypeScript 类型定义（Card, CardAttack, CardAbility 等）。实现了卡牌索引服务（card-service.ts），支持按 ID、名称、系列查询，支持模糊搜索、Standard 合规性过滤、多条件组合查询。编写了 21 个单元测试全部通过。

=== Session 2 | 2026-02-15 22:30:00 ===
Feature: 卡牌展示组件
Status: completed
Notes: 创建了 4 个核心组件：CardImage（图片展示，懒加载，错误回退）、CardDetail（详情面板，显示HP/技能/特性/弱点/合规性/flavor text）、CardGrid（响应式网格布局，点击展开详情）、CardSearch（搜索框+多维过滤：supertype/属性/系列/Standard）。新建 /cards 页面作为卡牌图鉴浏览器，支持分页加载（60张/页）。导航栏增加了"图鉴"链接。编写了 21 个组件测试全部通过，总计 44 个测试。

=== Session 3 | 2026-02-15 23:00:00 ===
Feature: 卡组导入与验证
Status: completed
Notes: 实现了 PTCG Live 格式卡组文本解析器（deck-parser.ts），支持 Pokemon/Trainer/Energy 分类解析、多词卡牌名处理。建立了 PTCG Live 系列代码到 pokemon-tcg-data set ID 的映射（SVI→sv1, PAL→sv2 等，覆盖 SV + SwSh 系列）。实现了卡组验证：60张检查、4张限制（基础能量豁免）、Standard 合规性标记、未知系列/未找到卡牌警告。创建了 DeckImport UI 组件（文本框粘贴+示例卡组+验证结果表格）。更新了 /deck 页面。编写了 23 个单元测试，总计 67 个测试全部通过。

=== Session 4 | 2026-02-16 03:30:00 ===
Feature: 游戏引擎核心 - 卡牌导入到对战系统的完整桥接
Status: completed
Notes: 解决了卡牌导入成功后无法在对战系统中使用的核心问题。

根因分析:
1. 导入的卡组数据仅存在于 DeckImport 组件的局部 state 中，页面跳转后丢失
2. 没有全局状态管理，卡组页和对战页之间无法通信
3. 没有游戏引擎类型定义，卡牌数据无法转化为可参与对战的游戏卡牌
4. 对战页面是空白占位符，没有卡组选择和游戏初始化逻辑

实现内容:
- deck-storage.ts: localStorage 持久化服务，支持保存/读取/删除卡组，提供 getDeckCardIds() 作为对战系统的主要接口
- DeckContext.tsx: React Context 全局状态管理，DeckProvider 包裹在根布局中，跨页面共享卡组数据
- engine/game-state.ts: 核心游戏状态类型（GameState, Player, Zone, GameCard），工厂函数和事件日志系统
- engine/zones.ts: 区域管理工具（洗牌/抽牌/移动/查找），Fisher-Yates 洗牌算法
- engine/battle-setup.ts: 卡组→对战桥接，loadDeckCards() 将存储的卡组 ID 解析为 GameCard 实例，initializeGame() 完成完整的对战初始化（洗牌→抽牌→奖励卡→开始回合）
- BattlePageClient.tsx: 对战页面客户端组件，支持卡组选择/对战初始化/游戏状态展示/事件日志
- 更新 DeckImport 和 DeckPageClient：导入后自动保存到 localStorage，显示已保存卡组列表
- 修复 CardSearch 组件 React 19 兼容性问题（useMemo→useEffect 避免渲染期间 setState）

测试: 新增 43 个测试（deck-storage 15个，game-engine 28个），总计 144 个测试全部通过。

=== Session 5 | 2026-02-16 09:00:00 ===
Feature: 对战准备阶段 - Mulligan 处理、奖励卡设置、基础宝可梦放置
Status: completed
Notes: 实现了完整的 PTCG 对战准备阶段流程。

实现内容:
- engine/battle-prepare.ts: 新建准备阶段模块，包含:
  - performSingleMulligan(): 单次 Mulligan（手牌洗回牌组，重抽 7 张）
  - resolveMulligans(): 完整 Mulligan 解决流程（双方同时处理，对手可额外抽牌，最大 20 轮安全限制）
  - flipCoin(): 掷硬币决定先攻（支持注入 randomFn 用于测试）
  - validatePlacement(): 验证基础宝可梦放置合法性（类型检查、数量限制、重复检查）
  - placeBasicPokemon(): 将手牌中的基础宝可梦放置到战斗区和备战区
  - autoPlaceBasicPokemon(): AI 自动放置策略（最高 HP 放战斗区，其余放备战区）
  - setPrizeCards(): 设置 6 张奖励卡
  - executePreparation(): 编排完整准备流程（Mulligan→放置→奖励卡→掷硬币→进入抽牌阶段）

- engine/battle-setup.ts: 重构 initializeGame()，新增 InitializeGameOptions 接口:
  - fullPreparation=true: 调用 executePreparation() 执行完整 PTCG 流程
  - fullPreparation=false (默认): 保持向后兼容的旧行为
  - 这样设计确保已有测试不受影响

- BattlePageClient.tsx: 增强对战界面:
  - 新增 PlayerField 子组件，显示战斗区（Active Pokemon）和备战区（Bench）
  - 新增 GameCardMini 子组件，展示宝可梦名称、HP、属性、技能、状态
  - 显示当前回合和阶段信息
  - 对战页面使用 fullPreparation: true

关键决策:
- initializeGame 使用 options 对象参数而非位置参数，便于扩展
- Mulligan 双方同时处理（符合官方规则），额外抽牌在最后一次性结算
- 自动放置策略选择最高 HP 作为战斗宝可梦，适合 AI 和快速开始模式

测试: 新增 38 个测试（battle-prepare.test.ts），总计 182 个测试全部通过（9 个测试套件）。

=== Session 6 | 2026-02-16 10:00:00 ===
Feature: 回合内动作实现 - 附能、进化、撤退、使用支持者/物品
Status: completed
Notes: 实现了完整的 PTCG 主阶段动作系统（turn-actions.ts）。

实现内容:
- attachEnergy(): 附加能量（每回合一次限制），支持战斗区和备战区目标
- evolvePokemon(): 进化宝可梦（首回合禁用、刚入场禁用、进化链检查），进化保留能量/伤害但清除状态异常
- retreat(): 撤退（消耗撤退费用丢弃能量，与备战区宝可梦交换）
- playSupporter(): 使用支持者（每回合一次限制）
- playItem(): 使用物品卡（不限次数）
- playBasicToBench(): 放置基础宝可梦到备战区（最多 5 只）
- drawCard(): 抽牌阶段抽牌（牌组为空触发 deck_out 败北）
- endTurn(): 结束回合（重置标记、切换玩家、推进回合数）

每个动作都有独立的 canXxx() 合法性检查和 xxx() 执行函数，全部记录事件日志。

测试: 新增 31 个测试（turn-actions.test.ts），总计 213 个测试全部通过（10 个测试套件）。

=== Session 7 | 2026-02-16 14:00:00 ===
Feature: 伤害与击倒系统 + 本地游戏控制器
Status: completed
Notes: 实现了完整的 PTCG 伤害计算和击倒系统，并构建了本地游戏控制器使 UI 操作能直接驱动引擎。

实现内容:
1. engine/game-actions.ts 增强:
   - calculateWeakness(): 弱点伤害计算（SV 时代 ×2）
   - calculateResistance(): 抵抗力减伤计算（-30）
   - calculateDamage(): 完整伤害公式（基础 × 弱点 - 抵抗力，最低 0）
   - getPrizeCount(): 多奖励卡规则（普通=1张，ex/V/VSTAR/GX=2张，VMAX=3张）
   - performAttack(): 重写完整攻击流程（伤害计算→弱点/抵抗→击倒判定→奖励卡→胜负判定→备战区提升）
   - promoteBenchPokemon(): 指定备战区宝可梦提升到战斗区
   - autoPromoteBench(): 自动提升第一只备战区宝可梦
   - concede(): 认输（触发 game_over）
   - canAttack(): 增加主阶段检查

2. engine/game-controller.ts（全新模块）:
   - processAction(): 主入口，路由所有玩家操作到对应引擎函数
   - 支持动作: play_card, attack, end_turn, evolve, retreat, promote, concede
   - 回合验证（不是你的回合时拒绝操作，promote/concede 例外）
   - 自动抽牌（结束回合后自动为下个玩家抽牌）
   - 攻击后自动结束回合并抽牌
   - KO 后自动提升（单只备战区宝可梦时）或提示用户选择
   - startFirstTurn(): 游戏开始时的首回合处理

3. engine/index.ts: 解决 turn-actions 和 game-actions 的导出冲突
   - 分别显式导出不冲突的函数
   - 注释说明冲突函数需从各自模块直接导入

4. BattlePageClient.tsx 重大更新:
   - 本地模式: 所有 UI 操作通过 GameController 路由到引擎
   - 在线模式: 通过 Socket.IO 发送操作（保持兼容）
   - 新增游戏结束覆盖层（显示获胜者、胜利条件、返回按钮）
   - startFirstTurn() 在本地对战初始化后自动处理首回合抽牌

关键决策:
- 伤害公式: (基础伤害 × 弱点倍率) - 抵抗力减免，符合官方规则
- 击倒前捕获 prize count（因为 checkKnockout 会将卡牌移入弃牌堆）
- 单只备战区宝可梦时自动提升，多只时通过 ActionResult 通知 UI 让玩家选择
- GameController 使用 shallow clone ({ ...state }) 触发 React 重新渲染
- TypeScript phase 类型缩窄问题通过 `as string` 解决（引擎函数会原地修改 phase）

测试: 新增 67 个测试（damage-knockout 48个，game-controller 19个），总计 280 个测试全部通过（12 个测试套件）。

=== Session 8 | 2026-02-16 ===
Feature: AI 对手 - 基于启发式评估的 SimpleBot
Status: completed
Notes: 实现了完整的 AI 对战系统，支持人类玩家 vs AI 的本地对战闭环。

实现内容:
1. engine/ai-player.ts（全新模块）:
   - computeAIAction(): 主入口，基于优先级的决策系统
   - computeAITurnActions(): 计算完整回合的动作序列
   - 8级优先级: promote → play basic active → play basic bench → evolve → attach energy → retreat → attack → end turn
   - 能量附加评分: active优先(+100)、需要能量攻击(+50)、高伤害目标加分
   - 攻击选择: 选择可用的最高伤害攻击（通过 canAttack 验证）
   - 撤退策略: active HP < 30% 且 bench 有更健康的宝可梦且能支付撤退费用
   - 进化策略: 优先进化 active 宝可梦
   - Promotion: 选择剩余 HP 最高的 bench 宝可梦

2. BattlePageClient.tsx 重大重写:
   - 新增 BattleMode 类型: "ai" | "local" | "online"
   - AI 模式: 人类(Player 0) vs AI(Player 1)，AI 自动回合执行
   - executeAITurn(): 递归执行 AI 动作，每个动作间 800ms 延迟
   - useRef 追踪最新 gameState，避免 setTimeout 闭包问题
   - AI 思考中指示器（脉冲动画）
   - AI 回合时玩家操作被锁定
   - 大厅 UI 重设计: AI 标签页/在线标签页
   - AI 卡组选择（可与人类用相同或不同卡组）
   - 正确的清理逻辑（unmount 和返回大厅时清除 timer）

3. 之前修复的 bug（同 session）:
   - 本地对战按钮 race condition: setState 异步但立即读取
   - 远程匹配: GameRoom.handleAction 改用统一的 processAction
   - DnD 能量附加: droppable data 格式修正
   - 本地模式视角: 动态 currentPlayerId

关键决策:
- AI 固定为 Player 1，棋盘始终从 Player 0 视角显示
- AI 动作之间加延迟模拟思考时间，提升用户体验
- 使用 computeAIAction 逐步执行而非批量执行，确保每个动作都基于最新状态
- 进化时优先进化 active（增强攻击能力）
- 打 Basic 到 bench 时优先低 HP（保留高 HP 的用于后续进化或撤退）

测试: 新增 32 个测试（ai-player.test.ts），总计 312 个测试全部通过（13 个测试套件）。

=== Session 9 | 2026-02-16 ===
Feature: 卡牌效果框架 - 动态效果加载系统
Status: completed
Notes: 实现了完整的卡牌效果框架，使对战中的卡牌效果（攻击特效、训练师、工具、特性、状态异常）真正生效。

实现内容:
1. engine/effects/effect-types.ts: 核心类型定义
   - EffectContext: 传递给效果函数的上下文，包含 20+ 工具方法（damage, heal, drawCards, searchDeck, flipCoin, applyStatus, switchOpponentActive 等）
   - AttackEffect/AttackResult: 攻击效果和结果（修改伤害、附带状态、bench伤害、自伤、能量丢弃）
   - AbilityEffect: 特性（activated/passive/on_enter 三种类型）
   - TrainerEffect: 训练师效果（canPlay 条件 + onPlay 执行）
   - ToolEffect: 工具效果（whileAttached 持续修改：伤害/撤退费/受击减伤）
   - CardEffectDef: 卡牌效果注册结构

2. engine/effects/effect-registry.ts: 效果注册中心
   - Map<string, CardEffectDef> 存储
   - registerEffect/registerAll/getEffect/hasEffect/clearRegistry

3. engine/effects/effect-context.ts: 上下文工厂
   - createEffectContext(): 创建 EffectContext 实例
   - 实现所有工具方法，每个方法操作 GameState 并记录 logEvent

4. engine/effects/coin.ts: 硬币翻转系统
   - flipCoin()/flipCoins() + 可注入 randomFn 用于测试

5. engine/effects/status-effects.ts: 状态异常回合间处理
   - processBetweenTurns(): 中毒(10伤害)、灼伤(20伤害+翻币治愈)、睡眠(翻币醒来)、麻痹(自动恢复)
   - KO 检查和奖励卡处理

6. engine/effects/cards/trainers.ts: 7种训练师效果（17个卡牌ID）
   - Boss's Orders（交换对手备战区）、Professor's Research（弃手抽7）、Pokemon Center Lady（治疗60+清状态）
   - Nest Ball（搜索基础宝可梦到备战区）、Energy Switch（移动能量）
   - Air Balloon（撤退费-2）、Sacred Charm（受击-30）

7. engine/effects/cards/attacks.ts: 10种攻击效果
   - 中毒攻击（Tangela, Shroodle）、硬币翻转伤害（Tyrogue）、自伤攻击（Corphish, Electrike）
   - 备战区伤害（Stonjourner）、能量丢弃攻击（Mega Camerupt ex, Kyogre）
   - 从牌组搜索能量附加（Exeggcute）、按能量数加伤害（Exeggutor）

8. 引擎集成:
   - game-actions.ts: performAttack() 重写，集成效果注册表查找、AttackResult 处理、工具伤害减免
   - turn-actions.ts: playSupporter/playItem 集成效果执行、endTurn 集成状态异常处理、canRetreat 集成工具减费
   - game-controller.ts: 新增 use_ability 动作类型和 handleUseAbility 处理器
   - game-state.ts: 新增 abilityUsedThisTurn 字段、4种新事件类型
   - ai-player.ts: 新增 Item/Supporter 使用优先级（Priority 5/7）

关键决策:
- 向后兼容: 无注册效果的卡牌走原有逻辑（parseInt baseDamage），312 旧测试不受影响
- 效果在 performAttack 的弱点/抵抗力计算之前执行，返回修改后的 baseDamage
- 训练师效果在移到弃牌堆之前执行（确保 searchDeck 等操作正确）
- 工具卡检测 subtype 包含 "Tool"，附加到目标宝可梦而非丢弃
- 状态异常互斥: asleep/confused/paralyzed 三者互斥，新状态覆盖旧状态

测试: 新增 39 个测试（card-effects.test.ts），总计 351 个测试全部通过（14 个测试套件）。

=== Session 10 | 2026-02-16 ===
Feature: 卡牌使用修复 - 训练师卡/能量系统/UI 增强
Status: completed
Notes: 修复了多个阻止训练师卡和攻击系统正常工作的关键问题。

修复内容:
1. battle-setup.ts: 在 initializeGame() 中调用 initializeEffects()
   - 之前效果注册表在运行时为空，所有注册的 trainer/attack 效果都不会执行
   - 现在每次创建游戏时自动初始化效果系统（Map.set 幂等，多次调用安全）

2. BattleBoard.tsx + Hand.tsx: 训练师卡点击使用
   - 之前 Item/Supporter 只能拖拽，但没有合适的放置目标，实际上无法使用
   - 新增 onCardClick 处理: 点击 Item/Supporter 直接触发 play_card action
   - Hand 组件新增 isMyTurn prop，非 Tool 的训练师卡在自己回合显示 "点击使用" 提示
   - Supporter/Item 有黄色高亮环标记可用状态

3. game-actions.ts: 能量类型校验 checkEnergyCost()
   - 之前 canAttack() 只检查附加能量总数 vs convertedEnergyCost，不检查类型
   - 新增 checkEnergyCost(attachedEnergy, cost) 函数，实现 PTCG 正式规则:
     * 特定类型费用（Fire, Water 等）必须用对应类型能量支付
     * Colorless 费用可用任意类型能量支付
     * 贪心算法: 先支付特定类型，剩余支付 Colorless
   - 新增 getEnergyType() 辅助函数: 从 GameCard 解析能量类型（Basic→types[0]，Special→Colorless）
   - canAttack() 改用 checkEnergyCost 替代简单计数检查

4. VisualCard.tsx: 能量/状态/工具显示
   - 新增 showEnergy prop，在宝可梦卡上叠加显示附加能量
   - 能量按类型分组显示（如 Rx2, W, C），每种类型用对应颜色圆角标签
   - 新增状态异常显示（毒/烧/眠/痹/混），用对应颜色标签
   - 新增工具卡指示器（右上角青色标签）

5. ActiveSpot.tsx: 攻击按钮能量费用显示
   - 攻击按钮现在显示: [能量费用圆点] [招式名] [伤害值]
   - 每个能量费用用对应颜色圆点表示（Fire=红, Water=蓝, Colorless=灰 等）
   - 能量不足的攻击灰显禁用（bg-zinc-600 + cursor-not-allowed）
   - 使用 checkEnergyCost() 实时判断每个攻击是否可用

6. BenchSpot.tsx: 备战区也显示能量
   - showEnergy={true} 传递给 VisualCard

7. turn-actions.ts: 修复 TypeScript 严格模式 phase 类型比较
   - processBetweenTurns 后 phase 可能被改为 game_over，用 as string 绕过类型窄化

关键决策:
- checkEnergyCost 独立导出: UI 组件可以直接调用判断攻击可用性，不需要创建完整 GameState
- 训练师卡使用: 点击而非拖拽，因为 Item/Supporter 不需要目标（Pokemon Tool 仍需拖拽到目标）
- 能量显示紧凑化: 同类型能量合并显示（如 Rx2 而非两个单独的 R 标签）

测试: 351 个测试全部通过，无新增测试（本次为 bugfix + UI 增强）。TypeScript 编译 0 错误。

=== Session 11 | 2026-02-16 ===
Feature: 对战 UI - PTCG Live 风格 UX 交互
Status: completed
Notes: 修复了训练师卡无法点击使用的根因，并实现了 PTCG Live 风格的完整交互系统。

根因分析:
- @dnd-kit 的 useDraggable hook 在外层 div 展开 {...listeners}，拦截所有 pointer 事件（onPointerDown）
- 导致内层 VisualCard 的 onClick 永远无法触发
- Session 10 添加的点击使用功能从未生效

修复与实现:
1. BattleBoard.tsx — PointerSensor + activationConstraint: { distance: 5 }
   - 普通点击不会移动 5px → onClick 正常触发
   - 拖拽手势自然超过 5px → 拖拽仍有效
   - 这是 @dnd-kit 官方推荐的点击+拖拽共存方案

2. BattleBoard.tsx — 选择状态 + 目标选择模式
   - selectedCardId/targeting 状态管理
   - computePlayableCardIds(): 计算当前回合可用的卡牌
   - handleMenuAction(): 路由卡牌动作（直接使用/进入目标选择模式）
   - handleTargetClick(): 目标选择完成后执行动作
   - Escape 键取消选择，回合切换自动重置选择

3. ActionMenu.tsx（新文件）— 浮动操作菜单
   - 根据卡牌类型显示不同操作: Item→使用, Supporter→使用, Tool→装备, Basic→放置, Stage→进化, Energy→附加
   - 效果未注册的训练师卡: 橙色 "!" 标记 + 警告文字

4. Hand.tsx — 可用/不可用/选中视觉状态
   - 可用卡: 黄色微光环，hover 上提
   - 不可用卡: 半透明 + 去饱和 + 禁止光标
   - 已选中卡: 大幅上提 + 蓝色高亮环
   - 目标选择模式: 手牌全部变暗

5. ActiveSpot.tsx + BenchSpot.tsx — 目标选择支持
   - 新增 isTargetable + onTargetClick props
   - 有效目标: 绿色发光边框 + 脉冲动画

6. Toast 通知系统
   - 效果未实现 → 橙色警告，操作失败 → 红色错误，3 秒消失

测试: 351 个测试全部通过。TypeScript 编译 0 错误。

=== Session 11b | 2026-02-16 ===
Feature: 修复"很多卡没法使用"的问题
Status: completed
Notes: 发现并修复了 game-controller.ts handlePlayCard() 中的路由顺序错误。

根因:
- handlePlayCard() 先按 targetZone 路由（"attach" → gaAttachEnergy），再按卡牌类型路由
- 导致 Pokemon Tool 被当作 Energy 处理（gaAttachEnergy 检查 supertype !== "Energy" 直接返回 false）
- 同样，任何带 targetZone="attach" 的非 Energy 卡都会被误路由

修复:
1. game-controller.ts: 重构 handlePlayCard() 路由逻辑
   - 改为先按 card.supertype 分流（Pokemon → Energy → Trainer），再按 targetZone 处理
   - Pokemon: Basic 自动检测 active/bench，Stage 1/2 支持 evolve（即使误作为 play_card 发送也能处理）
   - Energy: 需要 targetId 才能附加
   - Trainer: Tool→需 targetId→taPlayItem(tool)，Supporter→taPlaySupporter，Item→taPlayItem
   - 每种卡牌类型有明确的错误信息

2. BattleBoard.tsx: 新增 dispatchAction() 封装
   - 所有 onAction 调用改为 dispatchAction()
   - 自动捕获 ActionFeedback { success, error }，失败时显示 toast 通知
   - 用户终于能看到操作失败的原因了

3. BattlePageClient.tsx: handleLocalAction 返回 { success, error }
   - 之前只 console.warn，用户完全看不到错误
   - 现在返回结果供 BattleBoard 展示

测试: 351 个测试全部通过。TypeScript 编译 0 错误。

=== Session 12 | 2026-02-16 ===
Feature: 自动化集成测试 - 卡牌打出全路径覆盖
Status: completed
Notes: 编写了全面的集成测试，覆盖所有卡牌类型通过 processAction play_card 路由的完整路径。

实现内容:
- card-play-integration.test.ts（全新测试文件，37个测试）:

  1. Basic Pokemon Play（5个测试）:
     - 指定 bench/active 放置
     - 自动检测（active 空→active，否则→bench）
     - 备战区满时拒绝

  2. Energy Attachment（5个测试）:
     - 附加到 active/bench
     - targetZone=attach + targetId 路由正确
     - 缺少 targetId 报错
     - 每回合只能附加一次

  3. Supporter Play（3个测试）:
     - 成功打出进入弃牌堆
     - 每回合一张限制
     - 忽略 targetId（不需要目标）

  4. Item Play（2个测试）:
     - 成功打出进入弃牌堆
     - 不受每回合限制

  5. Pokemon Tool（5个测试）:
     - 装备到 active/bench
     - 缺少 targetId 报错
     - **核心回归测试**: targetZone=attach 不走能量路径（Session 11b 修复验证）
     - 已有工具时拒绝

  6. Evolution（4个测试）:
     - Stage 1 成功进化
     - 缺少 targetId 报错
     - 本回合刚入场禁止进化
     - 进化链不匹配报错

  7. Error Cases（5个测试）:
     - 非 main phase 拒绝
     - 对方回合拒绝
     - 缺少 cardId/不存在的 cardId
     - 游戏结束后拒绝

  8. Multi-Step Turn Flows（3个测试）:
     - 能量→物品→宝可梦同回合组合
     - 支持者→工具→结束回合流程
     - 5步完整回合: 宝可梦→能量→工具→支持者→物品→结束

  9. Card Type Routing Regression（5个测试）:
     - Tool + targetZone=attach → 走工具路径不走能量
     - Energy + targetZone=attach → 正确附加
     - Supporter + 错误 targetZone → 仍走支持者路径
     - Item + 错误 targetZone → 仍走物品路径
     - Basic + targetZone=attach → 自动检测放置

测试: 新增 37 个测试，总计 388 个测试全部通过（15 个测试套件）。TypeScript 编译 0 错误。

=== Session 13 | 2026-02-16 ===
Feature: 主流卡牌逻辑实现 (Priority 13)
Status: completed
Notes: 
  问题: 用户报告"卡牌使用后没有触发对应规则"。调查发现效果系统架构完好，但仅注册了 25 个效果（7 种训练师 + 10 种攻击），数据库有 248 种训练师名称。竞技常用卡（Iono、Ultra Ball、Switch 等）全部无效果实现。

  方案: 名称匹配 + 30 张效果

  实现步骤:
  1. effect-registry.ts — 新增名称注册表 (nameRegistry)
     - registerByName / registerAllByName
     - getEffect(cardId, cardName?) 支持名称回退
     - hasEffect(cardId, cardName?) 支持名称回退
     - clearRegistry() 同时清除两个 Map

  2. effect-types.ts + effect-context.ts — 8 个新 EffectContext 方法
     - searchDiscard(filter, count, who) — 从弃牌堆搜索并取出
     - shuffleHandIntoDeck(who) — 整个手牌洗入牌组
     - switchOwnActive(benchId) — 己方换人（不消耗撤退费）
     - revealTopCards(count, who) — 翻开牌组顶部 N 张
     - putOnTopOfDeck(cards, who) — 放回牌组顶部
     - shuffleIntoDeck(cards, who) — 指定卡牌洗入牌组
     - attachEnergyFromDiscard(filter, count, target) — 从弃牌堆附加能量
     - pickUpPokemon(instanceId, who) — 回收宝可梦+附件到手牌

  3. 更新所有 getEffect/hasEffect 调用点传入 cardName
     - turn-actions.ts: playSupporter, playItem, canRetreat (3处)
     - game-actions.ts: performAttack, applyToolDamageModifiers (2处)
     - game-controller.ts: handleUseAbility (1处)
     - ActionMenu.tsx + BattleBoard.tsx: hasEffect (7处)

  4. game-actions.ts — 攻击方工具 modifyDamage
     - 新增 applyAttackerToolDamageModifiers()
     - 在 performAttack 的弱点/抵抗力计算之前调用
     - 完整伤害链: base → attacker tools → weakness → resistance → defender tools → final

  5. trainers.ts — 30 个名称效果实现
     支持者(10): Iono, Arven, Nemona, Judge, Penny, Tulip, Lacey, Prof. Sada's Vitality, Prof. Turo's Scenario, Jacq
     物品(17): Ultra Ball, Switch, Night Stretcher, Buddy-Buddy Poffin, Super Rod, Pokémon Catcher, Counter Catcher, Great Ball, Poké Ball, Energy Search, Potion, Crushing Hammer, Earthen Vessel, Electric Generator, Pal Pad, Energy Retrieval, Rare Candy (stub)
     工具(3): Choice Belt (+30 vs ex/V), Vitality Band (+10), Defiance Band (+30 behind on prizes)
     + 7 个已有效果也注册了名称版本

  6. index.ts — 接线 trainerNameEffects + 导出新 API

  7. 测试: 新增 34 个测试 (trainer-effects-expanded.test.ts)
     - 名称注册表: registerByName, getEffect名称回退, ID优先级, clearRegistry
     - 新方法: searchDiscard, shuffleHandIntoDeck, switchOwnActive, revealTopCards, etc.
     - 训练师效果: Iono, Judge, Nemona, Ultra Ball, Switch, Potion, Night Stretcher, Energy Search, Pokémon Catcher (正/反面), Super Rod, Pal Pad
     - 工具伤害修改器: Vitality Band +10, Choice Belt +30 vs ex, Choice Belt 不加 vs 普通, Defiance Band +30 落后

  文件变更: 11 个文件
     modified: effect-registry.ts, effect-types.ts, effect-context.ts, trainers.ts, index.ts
     modified: turn-actions.ts, game-actions.ts, game-controller.ts
     modified: ActionMenu.tsx, BattleBoard.tsx
     new: trainer-effects-expanded.test.ts

  已知限制:
     - Rare Candy: stub — 需要 canEvolve() 支持跳阶进化
     - Kieran: +20 伤害需回合级修改器系统
     - Exp. Share / Heavy Baton: 需要 on-KO 触发钩子
     - 需要选择目标的效果自动选择第一个有效目标

测试: 新增 34 个测试，总计 422 个测试全部通过（16 个测试套件）。TypeScript 编译 0 错误。

=== Session 14 | 2026-02-16 ===
Feature: WebSocket 对战通信 (Priority 8)
Status: completed
Notes:
  基础设施已有: Socket.IO 服务器(server.ts)、GameRoom 类、SocketContext 客户端上下文。
  本 session 完成了在线对战的完整闭环。

  实现内容:
  1. game-room.ts — 状态遮蔽 (getMaskedState)
     - 隐藏对手手牌/牌组/奖励卡: 替换为 __hidden__ 占位卡（保留 zone 大小）
     - 隐藏自己的牌组/奖励卡（不能偷看牌序和奖励内容）
     - 公开区域（active, bench, discard）保持可见
     - JSON.parse(JSON.stringify) 深拷贝确保不污染权威状态
     - 新增 getPlayerIndex(), hasPlayer(), isGameOver() 辅助方法
     - 新增 handleDisconnect() — 掉线自动认输
     - 新增 createdAt/lastActivityAt 时间戳

  2. server.ts — 完整服务端通信
     - broadcastMaskedState(): 每个玩家收到各自视角的遮蔽状态
     - broadcastGameOver(): 各自遮蔽 + winner 信息
     - matchmaking:cancel 正确清除队列
     - disconnect 处理: 清除匹配队列 + 正在进行的游戏自动认输通知对手
     - game:action 成功后检查 promotionRequired → 发送 game:promotion_required 到特定玩家
     - game:reconnect 事件（重连支持，基于 socketId）
     - playerGameMap: socketId → gameId 快速查找
     - 30分钟过期自动清理（cleanupStaleGames 每 5 分钟扫描）
     - game:start 携带 gameId 供客户端保存

  3. BattlePageClient.tsx — 在线模式客户端闭环
     - 新增 onlineGameId 状态，所有 socket.emit 使用此 ID
     - onAction 在线模式: socket.emit("game:action", { gameId, action })
     - 监听 game:over → 更新 gameState（触发游戏结束覆盖层）
     - 监听 game:action_error → 红色 toast 提示 3 秒消失
     - 监听 game:promotion_required → 由 BattleBoard 的 bench 点击 UI 处理
     - 监听 error → 红色 toast
     - 胜负判定修复: iWon = winner.playerIndex === myPlayerId（支持 Player 1 视角）
     - 在线模式绿色脉冲指示器（右上角"在线对战"标签）
     - handleReturnToLobby 重置所有在线状态

  4. websocket-game-room.test.ts — 29 个新测试
     - 初始化: 成功初始化、空牌组失败、时间戳追踪
     - 状态遮蔽(8): 对手手牌/牌组/奖励卡隐藏、自己牌组/奖励卡隐藏、zone 大小保留、
       公开区域可见、反向遮蔽(Bob视角隐藏Alice手牌)、不污染权威状态、元数据保留
     - 玩家识别(2): getPlayerIndex、hasPlayer
     - 操作路由(5): 正确路由、未知socket拒绝、非当前玩家拒绝、状态更新、活跃时间更新
     - 掉线处理(3): 自动认输、未知socket无操作、已结束游戏无操作
     - 生命周期(2): 创建→认输完整流程、多回合对弈
     - 隐藏卡结构(2): 占位卡格式、instanceId 保留
     - 通信协议(3): ActionResult 结构、错误消息、GameState 结构

  文件变更: 5 个文件
     modified: server.ts, game-room.ts, BattlePageClient.tsx, feature_list.json
     new: websocket-game-room.test.ts

测试: 新增 29 个测试，总计 451 个测试全部通过（17 个测试套件）。TypeScript 编译 0 错误。

=== Session 15 | 2026-02-16 ===
Feature: 状态异常攻击/撤退检查（核心规则修复）
Status: completed
Notes:
  问题: 引擎未实现 PTCG 基本规则 — 麻痹/睡眠的宝可梦可以自由攻击和撤退，混乱无自伤判定。

  实现内容:
  1. game-actions.ts — canAttack() 增加状态异常检查
     - 麻痹 (paralyzed) → return false（不能攻击）
     - 睡眠 (asleep) → return false（不能攻击）
     - 注意: 混乱/中毒/灼伤不阻止攻击（canAttack 返回 true）

  2. game-actions.ts — performAttack() 增加混乱硬币翻转
     - 混乱 (confused) → 攻击前翻硬币
     - 正面: 攻击正常进行
     - 反面: 30 点自伤（3 个伤害计数器）+ 攻击失败
     - 自伤可以 KO 自己（触发奖励卡和胜负判定）
     - ex/V 宝可梦混乱自 KO 给对手 2 张奖励卡

  3. turn-actions.ts — canRetreat() 增加麻痹检查
     - 麻痹 → fail("麻痹状态的宝可梦不能撤退")
     - 注意: 睡眠/混乱/中毒/灼伤不阻止撤退

  关键规则（符合 PTCG 官方）:
  - 麻痹: 不能攻击 + 不能撤退（回合结束时自动解除）
  - 睡眠: 不能攻击，但可以撤退（付费后清除睡眠）
  - 混乱: 可以尝试攻击，翻硬币决定结果
  - 中毒/灼伤: 不影响攻击和撤退（伤害在回合间处理）

  文件变更: 3 个文件
     modified: game-actions.ts, turn-actions.ts
     new: status-attack-checks.test.ts

测试: 新增 29 个测试，总计 480 个测试全部通过（18 个测试套件）。TypeScript 编译 0 错误。

=== Session 15b | 2026-02-16 ===
Feature: PTCG 核心规则审计 + 4 个严重 Bug 修复
Status: completed
Notes:
  用户质疑引擎是否真正了解 PTCG 规则。做了完整的规则审计，发现 4 个严重错误：

  Bug 1 — 撤退不清除状态异常 [CRITICAL]
  - turn-actions.ts retreat() 函数未清除撤退宝可梦的状态
  - 修复: 在 swap 之前加 active.statusConditions = []
  - PTCG 规则: 任何宝可梦离开战斗区时清除所有状态

  Bug 2 — switchOpponentActive 不清除状态 [CRITICAL]
  - effect-context.ts switchOpponentActive() 未清除被换下宝可梦状态
  - 对比: switchOwnActive() 已正确实现清除
  - 修复: 加 currentActive.statusConditions = []

  Bug 3 — 回合结束只处理当前玩家的状态异常 [CRITICAL]
  - endTurn() 只调用 processBetweenTurns(currentPlayer)
  - PTCG 规则: Between Turns 双方活跃宝可梦都要处理状态
  - 场景: A 攻击使 B 中毒 → A 回合结束 → B 应该受 10 毒伤 → 但引擎没处理
  - 修复: 调用两次 processBetweenTurns，先当前玩家再对手

  Bug 4 — handleAttack 多 bench 升级路径提前调用 endTurn [HIGH]
  - 对手被 KO 后有多只 bench 时，endTurn 在升级选择前就执行了
  - 导致 between-turns 状态处理在宝可梦不完整时就运行
  - 修复: 移除 handleAttack 中的提前 endTurn，改到 handlePromote 中执行

  审计确认已正确实现的规则:
  - 回合结构、首回合限制、进化规则、能量附加限制
  - 无限次撤退（符合官方）、支持者限制、弱点/抵抗力
  - 奖励卡计数、三个胜利条件、KO 丢弃附件、能量类型检查

  文件变更: 4 个文件
     modified: turn-actions.ts, effect-context.ts, game-controller.ts
     new: rule-fixes-15b.test.ts

测试: 新增 21 个测试，总计 501 个测试全部通过（19 个测试套件）。TypeScript 编译 0 错误。

=== Session 15c | 2026-02-16 ===
Feature: 系统级规则修复 — 先攻方支持者限制 + 睡眠阻止撤退
Status: completed
Notes:
  基于 PTCG 官方规则手册深度审计，修复 2 个系统级规则违规：

  Fix 1 — 先攻方第一回合不能使用支持者卡 [CRITICAL]
  - canPlaySupporter() 缺少首回合限制
  - PTCG 规则: "The player who goes first cannot attack or play a Supporter card on their first turn."
  - 修复: 在 canPlaySupporter() 添加 state.turn===1 && state.isFirstTurn 检查
  - 与 canAttack() 的首回合攻击限制使用相同条件

  Fix 2 — 睡眠状态阻止撤退 [CRITICAL]
  - canRetreat() 只检查了麻痹，遗漏了睡眠
  - PTCG 规则: "If a Pokémon is Asleep, it can't attack or retreat."
  - 修复: 在 canRetreat() 麻痹检查后添加 asleep 检查
  - 修正了 Session 15 和 15b 中基于错误理解编写的 2 个测试用例

  文件变更: 4 个文件
     modified: turn-actions.ts (2 处修改)
     modified: status-attack-checks.test.ts (修正 1 个错误测试)
     modified: rule-fixes-15b.test.ts (修正 1 个错误测试)
     new: rule-fixes-15c.test.ts (16 个新测试)

测试: 新增 16 个测试，总计 517 个测试全部通过（20 个测试套件）。TypeScript 编译 0 错误。

=== Session 16 | 2026-02-16 ===
Feature: 运行时排查 + 关键修复 — 可能导致"无法对战"的 4 个嫌疑点
Status: completed
Notes:
  用户报告"无法正常对战"，尽管 517 个单元测试全通过。对 4 个嫌疑点逐一排查：

  嫌疑 1 — 动作日志侧边栏缺失导致 UI 崩溃 → ✅ 安全
  - UI 从未引用 ActionLog 组件，state.log 存在但不渲染
  - 不会导致任何崩溃

  嫌疑 2 — Socket.IO 通信链路断裂 → 🔴 关键问题
  - 发现: npm run dev (纯 Next.js) 和 npm run dev:socket (统一服务器) 是两个不同命令
  - server.ts 是统一 HTTP 服务器(Next.js + Socket.IO 共享端口 3000)
  - 但用户可能只运行 npm run dev，导致 Socket.IO 永远不启动
  - 修复: package.json 的 "dev" 改为指向统一服务器(ts-node server.ts)
  - 修复: SocketContext 添加 connect_error 事件处理，不再静默失败

  嫌疑 3 — 未实现卡牌效果导致注册表未命中 → ✅ 安全
  - 所有 getEffect() 使用 ?. 安全链，返回 null 时静默降级
  - 训练师卡无效果 → 打出后无效但不崩溃(卡牌正常弃置)
  - 攻击无效果 → 回退到卡面基础伤害
  - AI 只选择有注册效果的训练师卡使用

  嫌疑 4 — AI 决策死循环 → 🟡 中高风险
  - executeAITurn() 递归 setTimeout 无动作上限
  - 理论上某些边缘状态可导致无限循环
  - 修复: 添加 AI_MAX_ACTIONS_PER_TURN = 30 安全上限
  - 超过上限自动强制 end_turn

  验证:
  - 统一服务器 (npm run dev) 成功启动，HTTP 200
  - Socket.IO polling 端点正常响应(返回 sid + websocket upgrade)
  - 全部 566 个测试通过（21 个测试套件），0 TS 错误

  文件变更: 3 个文件
     modified: package.json (dev 指向统一服务器)
     modified: SocketContext.tsx (添加 connect_error 处理)
     modified: BattlePageClient.tsx (AI 动作上限 + 提示文字更新)

=== Session 17 | 2026-02-16 ===
Feature: 分级降级系统 (Graduated Degradation System)
Status: completed
Notes: 实现了三层防御系统解决"无法正常对战"问题：

  Layer 1 (P0) — 代理卡生成:
  - 新建 proxy-card.ts: createProxyCard() 从名称/setCode/number/category 推断 supertype/subtypes/HP
  - 名称启发式: ex→HP230, V→HP220, VMAX→HP330, Supporter/Tool 子类型推断
  - deck-storage.ts: 新增 getAllDeckCardEntries() 获取含未解析卡条目
  - battle-setup.ts: loadDeckCards() 新增 enableProxyCards 参数
  - VisualCard.tsx: 代理卡显示橙色 PROXY 标签 + 琥珀渐变背景
  - 20 个测试全部通过

  Layer 2 (P1) — 手动操作面板:
  - 新建 manual-override.ts: 14 种手动操作 (draw/discard/search/damage/heal/switch/evolve/energy/status)
  - game-controller.ts: 新增 manual_override action type，绕过回合检查
  - 新建 ManualToolkit.tsx: 折叠式侧边栏 UI，4 组操作按钮 + 参数表单
  - BattleBoard.tsx: 集成 ManualToolkit 组件 + 🔧 切换按钮
  - 21 个测试全部通过

  Layer 3 (P2) — 文本效果解析器:
  - 新建 text-parser.ts: ~15 种正则匹配模式 (攻击: 状态/自伤/硬币/丢弃能量/板凳伤害/每能量伤害/治疗; 训练师: 抽牌/弃牌抽牌/搜索/换人/治疗/双方洗牌)
  - effects/index.ts: initializeEffects() 接受可选 cards 参数触发自动注册
  - battle-setup.ts: 加载卡牌后调用 autoRegisterTextEffects()
  - 37 个测试全部通过

  测试: 567 → 644 (新增 78 个), 0 TS 错误
  文件变更: 20 files changed, 4461 insertions
  新文件: proxy-card.ts, manual-override.ts, text-parser.ts, ManualToolkit.tsx, evolution.middleware.ts + 4 个测试文件

=== Session 18 | 2026-02-17 ===
Feature: 动作日志侧边栏 (Action Log Sidebar) — Feature 11 (最后一个功能)
Status: completed
Notes: 实现了对战日志侧边栏，显示所有游戏事件：

  ActionLog 组件 (新建 ActionLog.tsx):
  - 左侧折叠式侧边栏 (absolute left-0 z-30 w-80)
  - 21 种事件类型 → 图标 + 颜色映射 (攻击=红, 抽牌=蓝, KO=橙, 状态=紫)
  - 回合分组显示 (groupEventsByTurn): 检测 "回合 N" 和 "第 N 回合" 模式
  - 自动滚动: 新事件到底部，向上滚动暂停，"↓ 最新" 按钮恢复
  - 玩家颜色区分: player0 蓝底, player1 红底
  - 导出纯函数 formatGameEvent/groupEventsByTurn 供测试

  AI 速度控制 (修改 BattlePageClient.tsx):
  - 4 档速度: 慢(1500ms)/中(800ms)/快(300ms)/瞬间(50ms)
  - aiSpeedRef 避免闭包问题
  - 速度选择器集成在日志侧边栏 header (仅 AI 模式显示)

  BattleBoard 集成:
  - 左侧 "📋 日志" 切换按钮 (蓝色主题)
  - 默认展开 (logOpen=true)
  - 传递 battleMode/aiSpeed/onAiSpeedChange props

  撤退验证:
  - 确认 retreatedThisTurn 检查已存在于 base-rules.ts:71 (checkHardRules)
  - turnStatus.retreated 在 retreat()设置、endTurn()重置、basePipeline 验证
  - 无需额外修改

  测试: 22 个新测试全部通过 (格式化 9 + 分组 6 + 撤退 3 + 集成 4)
  注意: 4 个测试套件 (44 个测试) 因前一 session 未提交的验证管道重构而失败
        (game-controller.ts 的 basePipeline 替换旧的 inline 验证)，非本 session 引入

  文件变更: 19 files, +1637 lines
  新文件: ActionLog.tsx, action-log.test.ts, CardDetailModal.tsx, CardSelectionModal.tsx,
         validation.ts, base-rules.ts, attack-system.ts + 2 个引擎测试文件

=== Session 19 | 2026-02-17 ===
Feature: ryuu-play 引擎移植 + 适配层
Status: completed
Notes:
  从 GitHub 克隆并评估了 ryuu-play (keeshii/ryuu-play) 开源 PTCG 引擎（203 specs 全通过），
  确认支持 Energy Attachment、Evolution、Damage Calculation 三大核心机制。

  实现内容:
  1. 引擎移植:
     - 复制 ryuu-play packages/common/src → app/src/lib/ptcg-engine/（核心引擎）
     - 复制 ryuu-play packages/sets/src → app/src/lib/ptcg-sets/（卡牌集）
     - 配置 TypeScript 路径别名 @ptcg/common → ./src/lib/ptcg-engine/index.ts
     - 排除 ryuu-play spec 文件（Jasmine）避免与 Jest 冲突

  2. 适配层:
     - app/src/adapters/engine-adapter.ts: 完整的类型桥接
       - mapRyuuStateToUI(): State → GameState 主转换器
       - ryuuCardToUICard(): 类实例 → 纯接口
       - pokemonSlotToGameCard(): PokemonSlot → GameCard（含能量/工具/伤害/状态）
     - app/src/lib/ryuu-adapter/: 外部类型适配（修复 TypeScript 类型不匹配）

  3. 异步测试修复（86 个测试）:
     - processAction/playSupporter/playItem 改为 async 后，9 个测试文件需要添加 async/await
     - 5 个并行 agent 同时修复所有测试文件
     - handleAction/handleDisconnect（game-room.ts）也需异步化测试

  测试: 27 套件，678 测试全部通过。TypeScript 编译 0 错误。
  文件变更: 894 files changed, 69489 insertions, 507 deletions

=== Phase 1 完成报告 ===
所有 14 个功能均已完成（passes: true）。
总测试: 27 套件，678 测试全部通过。

=== Session 20 | 2026-02-17 ===
Feature: Stadium 卡系统 — 场地卡机制 (Priority 15)
Status: completed
Notes:
  Phase 2 启动，新增 10 个功能到 feature_list.json (P15-P24)。
  本 session 实现了完整的场地卡系统。

  实现内容:
  1. game-state.ts — GameState 新增 stadium 字段
     - stadium: { card: GameCard; owner: 0 | 1 } | null
     - 新增 play_stadium / remove_stadium 事件类型
     - createGameState() 初始化 stadium: null

  2. turn-actions.ts — playStadium() 场地卡打出
     - canPlayStadium(): 主阶段检查、一回合一张限制、同名限制
     - playStadium(): 替换旧场地(丢弃到旧owner弃牌堆)、放置新场地、执行效果
     - canRetreat(): 集成 Beach Court 场地效果 (Basic 撤退费 -1)

  3. game-controller.ts — 路由 Stadium 子类型
     - handlePlayCard 新增 subtypes.includes("Stadium") 分支
     - 调用 taPlayStadium()

  4. effect-types.ts + effect-context.ts — EffectContext 新增方法
     - getStadium(): 获取当前场地卡 (GameCard | null)
     - removeStadium(): 移除场地并丢弃到 owner 弃牌堆

  5. effects/cards/stadiums.ts — 5 张场地卡效果注册
     - Beach Court: Basic 撤退费 -1 (via canRetreat 直接检查)
     - Artazon: 搜索非 Rule Box 基础宝可梦
     - Iono's Palace: 展示手牌迫使对手洗手抽同数
     - Academy at Night: 手牌放回牌组顶
     - Mesagoza: 弃手抽 5
     - 均为持续效果(onPlay 空), 注册到 nameRegistry

  6. UI 集成
     - ActionMenu.tsx: 新增 "打出场地" 操作按钮
     - BattleBoard.tsx: Stadium 显示区(中间状态栏翡翠标签)、playability 检查
     - ActionLog.tsx: play_stadium/remove_stadium 事件图标

  7. AI 集成
     - ai-player.ts: Priority 7.5 — 自动打出手中的场地卡(避免同名)

  8. TypeScript 修复
     - text-parser.test.ts: mock EffectContext 添加 getStadium/removeStadium
     - engine-adapter.ts + ryuu-adapter: 添加 stadium: null

  测试: 新增 27 个测试 (stadium-system.test.ts)，总计 705 测试全部通过 (28 套件)。TypeScript 编译 0 错误。
  文件变更: 12 files modified, 1 new test file, 1 new effects file

=== Session 21 | 2026-02-17 ===
Feature: ryuu-play 卡牌效果批量移植 — 50 张 Standard 核心卡效果 (Priority 16)
Status: completed
Notes:
  分析了 ryuu-play 717 张卡牌实现，发现均为旧世代卡(Base/Fossil/EX/BW)，
  不适合直接移植。改为以 ryuu-play 模式为参考，使用 EffectContext API 编写 SV 时代效果。

  实现内容:
  1. meta-attacks.ts — 26 个 Pokemon 攻击效果 (16 张核心 meta 宝可梦)
     - Charizard ex: Burning Dark (180 + prizesTaken*30)
     - Pidgeot ex: Blustering Gale (120 + removeStadium)
     - Dragapult ex: Phantom Dive (200 + 60 bench spread)
     - Terapagos ex: Crown Opal (benchCount*30)
     - Gardevoir ex: Psychic Force (190 + removeAllStatus)
     - Roaring Moon ex: Frenzied Gouging (KO self + 200 self), Calamity Storm (100+120 if stadium)
     - Chien-Pao ex: Hail Blade (60*waterEnergyDiscarded)
     - Raging Bolt ex: Burst Roar (discard hand draw 6), Bellowing Thunder (70*basicEnergyDiscarded)
     - Snorlax: Toss and Turn (30*coinFlips), Collapse (150)
     - Comfey: Flower Selecting (both draw 3)
     - Munkidori: Adrena Brain (100+100 if status)
     - Iron Thorns ex: Electro Hammer (160)
     - Dusknoir: Special Punch (150 + preventRetreat)
     等共 26 个命名攻击效果

  2. trainers-expanded.ts — 24 个额外训练师效果
     支持者(11): Kieran, Eri, Crispin, Perrin, Roxanne, Colress's Tenacity, Cyllene, Worker, Serena, Adventurer's Discovery, Giovanni's Charisma
     物品(6): TM:Evolution, Techno Radar, Canceling Cologne, Lost Vacuum, Hisuian Heavy Ball, Prime Catcher
     工具(5): Maximum Belt(+50 vs ex/V), Forest Seal Stone, Bravery Charm, Leftovers, Hero's Cape

  3. effects/index.ts — 注册新效果模块
     - registerAllByName(metaAttackEffects)
     - registerAllByName(expandedTrainerEffects)
     - getBuiltInEffectCount() 更新

  4. meta-effects-integration.test.ts — 31 个集成测试
     - 注册验证(3), Charizard ex(2), Pidgeot ex(1), Dragapult ex(1), Terapagos ex(2),
       Gardevoir ex(1), Roaring Moon ex(2), Chien-Pao ex(2), Snorlax(1), Comfey(1),
       Raging Bolt ex(2), Eri(1), Roxanne(2), Crispin(1), Worker(1), Maximum Belt(2),
       Perrin(1), Lost Vacuum(1), Munkidori(2), Iron Thorns ex(1), Dusknoir(1)

  测试: 新增 58 个测试 (meta-effects-integration.test.ts), 总计 736 测试全部通过 (29 套件)。TypeScript 编译 0 错误。
  文件变更: 3 new files (meta-attacks.ts, trainers-expanded.ts, meta-effects-integration.test.ts), 1 modified (effects/index.ts)

=== Session 22 | 2026-02-17 ===
Feature: Marker 系统 — 卡牌标记/计数器支持 (Priority 17)
Status: completed
Notes:
  实现了持久标记系统，支持跨回合效果追踪（如"下回合不能攻击"、"不能撤退"、"VSTAR已使用"等）。

  实现内容:
  1. game-state.ts — GameCard 新增 markers: Record<string, number> 字段
     - createGameCard() 初始化 markers: {}
     - 所有构造 GameCard 的位置都已更新 (engine-adapter, ryuu-adapter, game-room)

  2. effect-types.ts + effect-context.ts — 4 个 marker CRUD 方法
     - addMarker(target, name, count?): 增加标记计数（默认 +1）
     - removeMarker(target, name, count?): 减少或删除标记
     - getMarker(target, name): 获取标记值（0 如果不存在）
     - hasMarker(target, name): 检查标记是否存在

  3. markers.ts — 标记常量（NEW）
     - CANT_ATTACK_NEXT_TURN: 下回合不能攻击
     - PREVENT_RETREAT_NEXT_TURN: 下回合不能撤退
     - cantUseAttackMarker(name): 不能使用特定攻击
     - VSTAR_USED, ABILITY_BLOCKED, DAMAGE_BOOST: 永久标记
     - TURN_BASED_MARKERS: 需要回合间递减的标记列表

  4. status-effects.ts — processBetweenTurns() 增加标记清理
     - 回合间自动递减 TURN_BASED_MARKERS 和 CANT_USE_ATTACK:* 标记
     - 达到 0 时删除标记
     - 活跃/备战区宝可梦都处理
     - 修复了早期返回条件（无状态异常时仍需处理标记）

  5. game-actions.ts — canAttack() 增加标记检查
     - CANT_ATTACK_NEXT_TURN > 0 → 所有攻击被阻止
     - cantUseAttackMarker(attackName) > 0 → 特定攻击被阻止

  6. turn-actions.ts — canRetreat() 增加标记检查
     - PREVENT_RETREAT_NEXT_TURN > 0 → 撤退被阻止
     - evolvePokemon() 进化时清空所有标记

  7. meta-attacks.ts — 6 处 ctx.log() 替换为 ctx.addMarker()
     - Miraidon ex (Photon Blaster): addMarker CANT_ATTACK_NEXT_TURN
     - Koraidon ex (Wild Impact): addMarker CANT_ATTACK_NEXT_TURN
     - Bloodmoon Ursaluna ex (Blood Moon): addMarker CANT_ATTACK_NEXT_TURN
     - Dusknoir (Shadow Bind): addMarker PREVENT_RETREAT_NEXT_TURN on defender
     - Greninja ex (Shinobi Blade): addMarker cantUseAttackMarker("Shinobi Blade")

  8. VisualCard.tsx — 左侧标记徽章显示
     - 红色: 攻击封锁(封)/限制(限)/撤退锁定(锁)
     - 蓝色: 特性禁用(禁)
     - 黄色: VSTAR已使用(V★)/其他标记

  9. 测试兼容性
     - text-parser.test.ts mock 添加 4 个 marker 方法
     - effect-context.ts pickUpPokemon() 清空 markers

  测试: 新增 35 个测试 (marker-system.test.ts), 总计 771 测试全部通过 (30 套件)。TypeScript 编译 0 错误。
  文件变更: 2 new files (markers.ts, marker-system.test.ts), 10 modified

=== Session 23 | 2026-02-17 ===
Feature: 移动端响应式布局 — 适配手机和平板
Status: completed
Notes:
- 创建 useIsMobile hook (matchMedia, 默认 768px breakpoint, SSR safe)
- BattleBoard: 导入 useIsMobile, 根据 isMobile 传递 compact prop 给所有子组件
  - 对手信息栏: 移动端缩小头像、隐藏详细数据
  - 中间状态栏: 移动端高度 h-10, 紧凑信息显示 (T{n} · name)
  - 日志/工具按钮: 移动端仅显示图标, 隐藏工具按钮
  - bench gap: 移动端 gap-1 确保 5 张 bench 在 375px 内
  - 添加 TouchSensor (200ms delay) 支持移动端拖拽
  - log 默认在移动端关闭, viewport 变小时自动关闭
- ActiveSpot: compact 模式 150×108px (scale 0.65), 攻击按钮 w-[120px]
- BenchSpot: compact 模式 95×68px (scale 0.42)
- Hand: compact 模式 scale 0.55, 水平滚动 carousel + snap-x + touch-pan-x + 隐藏滚动条
- ActionMenu: compact 模式渲染为 fixed 底部 sheet (圆角顶部, 拖拽手柄, 更大按钮)
- ActionLog: compact 模式渲染为全屏 modal (fixed inset-0)
- 编写 9 个响应式测试 (useIsMobile hook + 组件尺寸契约 + 375px viewport 适配)
- 31 suites, 780 tests all passing, TypeScript clean

=== Session 24 | 2026-02-17 ===
Feature: 卡牌动画系统 — Framer Motion 对战动画 (Priority 19)
Status: completed
Notes:
  安装 framer-motion 并实现了完整的对战动画系统。

  实现内容:
  1. AnimationProvider.tsx (NEW) — 动画中央调度器
     - React Context + FIFO 队列，逐个处理 AnimationEvent
     - 7 种动画类型: draw/play/attack/hit/knockout/evolve/energy_attach
     - speedMultiplier 与 AI 速度联动: slow=1.5, normal=1.0, fast=0.5, instant=0.05
     - skipAll() 跳过所有待处理动画
     - 每种动画有独立的基础时长 (300-600ms)

  2. VisualCard.tsx — 受击震动 + HP 弹簧动画 + 浮动伤害
     - motion.div 替代原 div，isShaking 状态驱动 x 轴震动 [0,-5,5,-3,3,0]
     - HP 条使用 motion.div + spring 过渡 (stiffness:100, damping:15)
     - 浮动伤害数字改用 AnimatePresence + motion.div (从中心飘向上方渐隐)

  3. Hand.tsx — 抽牌/打出动画
     - AnimatePresence mode="popLayout" 包裹手牌列表
     - 新卡进入: initial={{ y:-40, opacity:0, scale:0.8 }} → spring 弹入
     - 卡牌打出/离开: exit={{ y:-30, opacity:0, scale:0.8 }}
     - layout 属性实现卡牌重排平滑过渡

  4. ActiveSpot.tsx — 攻击冲刺 + 击倒退场
     - AnimatePresence mode="wait" 包裹卡牌区域
     - 新卡入场: initial={{ scale:0.5, opacity:0 }} → spring 弹入
     - 攻击动画: isAttacking 驱动 x/y 偏移冲刺 (方向按对手/己方区分)
     - 击倒退场: exit={{ rotateX:90, opacity:0, y:30 }}
     - 新增 isAttacking prop

  5. BenchSpot.tsx — 入场/击倒动画
     - AnimatePresence mode="wait" 包裹卡牌
     - 入场: spring 缩放弹入
     - 击倒: rotateX 翻转退场

  6. EvolutionOverlay.tsx (NEW) — 进化闪光效果
     - 全屏绿色闪光 + "进化! {name}" 文字缩放渐隐
     - 由 evolve_pokemon 游戏日志事件触发

  7. EnergyAttachOverlay.tsx (NEW) — 能量飞行动画
     - 彩色能量圆点从手牌区飞向场地中央
     - 颜色按能量类型映射 (Fire=红, Water=蓝 等)
     - spring 物理弹性飞行轨迹

  8. BattleBoard.tsx — 动画集成
     - AnimationProvider 包裹 DndContext
     - 攻击动画: 手动触发 (己方) + 日志检测 (对手/AI)
     - 进化/能量动画: 通过游戏日志新事件自动触发
     - attackingPlayer state 管理攻击动画目标

  DnD Kit 共存策略:
  - Framer Motion 的 motion.div 与 DnD Kit 的 useDroppable 共存无冲突
  - 拖拽期间 DragOverlay 独立渲染，不受 AnimatePresence 影响
  - 使用 enter/exit 动画 (非 layoutId) 避免与 DnD Kit transform 冲突

  测试: 新增 30 个测试 (animation-system.test.ts), 总计 810 测试全部通过 (32 套件)。TypeScript 编译 0 错误。
  文件变更: 3 new files (AnimationProvider.tsx, EvolutionOverlay.tsx, EnergyAttachOverlay.tsx, animation-system.test.ts), 5 modified (VisualCard.tsx, Hand.tsx, ActiveSpot.tsx, BenchSpot.tsx, BattleBoard.tsx)

=== Session 25 | 2026-02-17 ===
Feature: PTCG 规则审计与修正 — 攻击回合终结 + 进化链堆叠
Status: completed
Notes:
  用户提供完整的 PTCG 规则审计清单，对引擎进行全面审计后确认：

  已正确实现的规则 (无需修改):
  - 奖赏卡区 (prizes: 6张, KO拿取, 拿完获胜) ✅
  - 牌库区 (deck: 抽牌来源, 耗尽判负) ✅
  - 游戏准备阶段 (洗牌→抽7张→Mulligan→放基础→奖赏卡) ✅
  - 特殊状态 (毒/烧/睡/麻/乱) 及互斥规则 ✅
  - 回合间检测 (Pokemon Checkup, 双方活跃都处理) ✅
  - 首回合限制 (先攻不能攻击/不能用支持者) ✅
  - 进化限制 (首回合禁/刚上场禁/同回合禁) ✅
  - 撤退 (每回合1次, 麻痹/睡眠阻止, 清除状态) ✅
  - 三个胜利条件 (奖赏拿完/无宝可梦/牌库抽干) ✅
  - 物品卡不限次数 ✅
  - 弱点 ×2 / 抵抗力 -30 ✅

  修正内容:
  1. 攻击作为回合结束动作 — hasAttackedThisTurn 标记
     - game-state.ts: TurnState 新增 hasAttackedThisTurn 字段
     - game-actions.ts: performAttack() 设置 hasAttackedThisTurn = true
     - game-actions.ts: canAttack() 检查 hasAttackedThisTurn
     - base-rules.ts: ATTACK_LIMIT 阻止重复攻击
     - base-rules.ts: ATTACK_ENDS_TURN 阻止攻击后的其他主阶段操作
     - turn-actions.ts: endTurn() 重置 hasAttackedThisTurn = false

  2. 进化链/堆叠记录 — evolutionStack 字段
     - game-state.ts: GameCard 新增 evolutionStack: Array<{cardId, card}>
     - turn-actions.ts: evolvePokemon() 进化前将当前卡推入 evolutionStack
     - effect-context.ts: pickUpPokemon() 清空 evolutionStack
     - 所有 GameCard 构造点 (5处) 添加 evolutionStack: []
     - 支持退化喷雾等效果读取底层卡信息

  3. 更新所有适配层构造函数
     - engine-adapter.ts (2处): 添加 evolutionStack + hasAttackedThisTurn
     - ryuu-adapter/adapter.ts: 添加 evolutionStack + hasAttackedThisTurn
     - server/game-room.ts: hidden card 添加 evolutionStack

  测试: 新增 46 个测试 (rule-corrections-25.test.ts)，覆盖:
    - 攻击回合终结 (9): hasAttackedThisTurn 设置/重置/阻止重复攻击/阻止攻击后操作
    - 进化堆叠 (5): 空栈/单次进化/双重进化/保留属性/清除状态
    - 区域完整性 (4): 奖赏卡/牌库耗尽/无宝可梦/奖赏拿完
    - 特殊状态 (8): 互斥/共存/撤退清除/麻痹阻攻/睡眠阻攻/麻痹阻退/睡眠阻退/毒伤/麻痹恢复
    - 首回合限制 (3): 不能攻击/不能用支持者/解除限制
    - 操作限制 (4): 能量1次/支持者1次/撤退1次/备战区5只
    - 多奖赏 (5): 普通1张/ex 2张/V 2张/VMAX 3张/VSTAR 2张
    - 控制器集成 (2): 攻击自动结束回合/重置状态
    - 字段初始化 (2): TurnState/GameCard 新字段

  总计: 33 套件，856 测试全部通过 (810 → 856)。TypeScript 编译 0 错误。
  文件变更: 9 modified, 1 new test file

=== Session 25b | 2026-02-17 19:25:00 ===
Feature: UI 规则体现 — 让规则对玩家可见
Status: completed
Notes:
  审计了全部 UI 组件，发现多个关键规则未在前端反映:
  - 无撤退按钮（核心机制缺失）
  - 攻击禁用原因不透明
  - 进化堆叠不可见
  - 攻击后状态（回合结束）未提示
  - 回合操作限制（附能/支持者/撤退/攻击）无状态栏

  修改文件:
  1. ActiveSpot.tsx (全面重写):
     - 新增撤退按钮，显示撤退费用（灰色圆点），状态禁用原因
     - 攻击按钮显示能量费用（彩色圆点），禁用时显示原因
       (已攻击/先攻/麻痹/睡眠/效果封锁/能量不足)
     - 进化堆叠指示器（左下角徽章+悬浮详情）
     - "已攻击—等待回合结束" 提示
     - 新增 props: gameState, playerIndex, onRetreat, hasAttackedThisTurn

  2. BattleBoard.tsx:
     - 传递新 props 给 ActiveSpot (gameState, playerIndex, hasAttackedThisTurn, onRetreat)
     - 实现 handleRetreat() 流程: 点击撤退 → 选择备战区宝可梦 → 自动选择能量支付 → dispatch
     - 撤退选择模式 (retreatSelecting): 备战区宝可梦高亮可点击
     - 撤退选择横幅: "🔄 选择备战区宝可梦来替换"
     - 回合状态栏 (桌面端): ⚡附能 / 📜支持者 / 🔄撤退 / ⚔攻击完毕
     - 回合状态栏 (移动端): 彩色圆点指示器
     - computePlayableCardIds() 增加 hasAttackedThisTurn 检查（攻击后手牌全灰）

  测试: 856 全通过，TypeScript 0 错误。
