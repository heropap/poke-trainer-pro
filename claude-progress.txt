=== Session F0 (tabula rasa) | 2026-05-03 ===
Feature: 清场 + 重建骨架（priority 0）
Status: completed
Notes:
  - 用户决策：完全重置 (tabula rasa)，从三套官方大师卡组（喷火龙 ex / 密勒顿 ex /
    沙奈朵 ex）+ 单人 PvE + 桌面浏览器 + PTCG Live 风格 UI 起步。
  - 已把以下旧资产移到 archive/legacy-2026-05-03/（git mv 跟踪历史）：
      * 根目录：spec.txt / feature_list.json / claude-progress.txt / plan.md /
        Poke-Trainer-Pro_Agent-Teams设计方案.md / init.sh / card test/ /
        files (1).zip
      * app/：src/ / server.ts / tsconfig.server.json / PROJECT_ARCHITECTURE.md /
        scripts/ / docs/
  - 已修补：
      * app/package.json：dev 脚本改为 next dev，删除 generate-rules
      * app/tsconfig.json：删除 @ptcg/common paths 与遗留 exclude
      * app/jest.config.ts：删除 @ptcg/common moduleNameMapper 与 ptcg-engine 忽略
      * app/next.config.ts：删除 /battle/page 的旧 redirect 与 scrydex.com 域名
  - 已新建：
      * app/src/app/layout.tsx / page.tsx / globals.css
      * app/src/app/api/health/route.ts（GET 返回 status=ok）
      * feature_list.json（15 条 v0 feature，priority 0-14）
      * spec.txt（v0 精简规格 ~100 行）
      * init.sh（启动 dev / 健康检查 / 跑测试 / 收尾）
  - 引擎、UI、卡牌数据全部为零；F1 起逐 feature 接入。
  - 开放问题待用户答复（详见 plan 文件「开放问题」节）：
      1. 三套大师卡组的官方卡表来源
      2. 卡图获取方式（pokemontcg.io 自动拉取 vs 用户提供官方中文版）
      3. AI 难度级别
      4. v0 完结后是 PvP 还是扩卡
      5. 音效是否预留挂载点

=== Session F1 | 2026-05-03 ===
Feature: 卡牌数据骨架 + 喷火龙 ex 大师卡组（priority 1）
Status: completed
Notes:
  - 新建 app/src/core/types.ts：CardDef union（Pokemon / Trainer / Energy）+ DeckDef + 9 种 EnergyType
  - 新建 app/src/core/cards.ts：Map registry + registerCard / getCard / validateDeck
  - 新建 app/src/core/decks/charizard-ex.ts：17 张 unique CardDef + DeckDef（slug=charizard-ex）
  - 卡图通过 pokemontcg.io 远程 URL（next.config.ts 已配 remotePatterns）
  - 8 个单元测试全过；init.sh 全绿
  - 卡表参考归档版 archive/.../prebuilt-decks.ts；标准 SV 时代 Charizard ex / Pidgeot ex 引擎主线，
    与中国官方大师卡组的具体卡数可能略有出入，待用户提供官方卡表后再 reconcile。

=== Session F2 | 2026-05-03 ===
Feature: 密勒顿 ex + 沙奈朵 ex 大师卡组（priority 2）
Status: completed
Notes:
  - 新建 app/src/core/decks/miraidon-ex.ts：10 张 unique CardDef + 60 张 deck list
  - 新建 app/src/core/decks/gardevoir-ex.ts：10 张 unique CardDef + 60 张 deck list
  - 修改 app/src/core/decks.ts：DECKS 暴露 3 套
  - 13 个单测全过；init.sh 全绿
  - 偏离归档版地方：
      Miraidon 把 2 Regieleki VMAX 替换成 2 Iron Hands ex (PAR 70)，
      避免 VMAX 没有对应 V 的非法 deck 构造（vMAX 必须有同名 V）。
      Iron Hands ex 是真实的 SV 时代 Miraidon 顶级搭档，Standard 合法。
  - 三套卡组共 37 张 unique 卡牌（Charizard 17 + Miraidon 复用 + Gardevoir 复用 + 新增）

=== Session F3 | 2026-05-03 ===
Feature: 游戏状态 + 阶段机 + 起手抽牌（priority 3）
Status: completed
Notes:
  - core/state.ts：GameState / PlayerState / GameCard / GamePhase / Prompt / StatusCondition；
    BENCH_SIZE=5 / HAND_DRAW=7 / PRIZE_COUNT=6
  - core/actions.ts：Action union（GameStart / PlaceActiveSetup / PlaceBenchSetup /
    DrawCard / ResolvePrompt / Concede）+ PromptResponse
  - core/rng.ts：mulberry32 seedable RNG + Fisher-Yates shuffle
  - core/reducer.ts：完整 setup 流程 (GameStart → mulligan + draw 7 → prompt
    selectActive → prompt selectBench → 切换玩家 → 翻 6 prizes → phase=draw)
  - autoSetup() helper：自动响应 setup 提示，用于测试和 AI（F7 会复用）
  - 12 个新单测全过；init.sh 全绿
  - Mulligan：手牌无 Basic 时 reshuffle 重抽（不实现「对手多抽 1 张」补偿；F4+ 再补）

=== Session F4 | 2026-05-03 ===
Feature: 主回合 actions（priority 4）
Status: completed
Notes:
  - actions.ts 扩充 9 个新 action：StartTurn / EndTurn / PlayBasicPokemon /
    AttachEnergy / Retreat / Evolve / PlayItem / PlaySupporter / PlayStadium /
    AttachTool
  - reducer.ts 实现：
      * StartTurn：抽 1（deck out 判断）→ 重置 perTurn flags → phase=main
      * EndTurn：切换 activePlayer / +1 turn / phase=draw
      * PlayBasicPokemon：手 → 后场（合法性 + 空槽校验）
      * AttachEnergy：手 → 前/后场（每回合一次锁）
      * Evolve：手中进化卡 → 已在场（首回合禁止 / 同回合放出禁止 / evolveFrom 名称匹配）
      * Retreat：支付精确 retreat cost / 状态封锁 / swap 前后场 / 弃能量
      * PlayItem：弃 + 派发到 effect registry（F4 注册表为空，F6 填）
      * PlaySupporter：每回合一次 + 首回合禁止
      * PlayStadium：替换在场 stadium，旧的进 owner 弃牌堆
      * AttachTool：唯一槽位 + 校验
  - core/effects.ts：CardId → EffectHandler 注册表（F4 仅基础设施，F6 注册具体效果）
  - 12 个新单测全过；4 个 test suite，共 37 tests；init.sh 全绿

=== Session F5 | 2026-05-03 ===
Feature: 攻击 + 伤害 + KO + 取奖励（priority 5）
Status: completed
Notes:
  - 新 actions：Attack / PromoteFromKO + 新 prompt kind: promoteFromKO
  - reducer 实现：
      * canPayAttackCost：能量花费校验（先匹配特定属性，剩余满足 Colorless）
      * calculateDamage：base × 2 weakness / -30 resistance
      * KO 检测：damage >= HP → 弃牌 + 取奖励（normal 1 / ex&V&VSTAR 2 / VMAX 3）
      * 取奖励后判断胜利（prizes 空 → 胜利）
      * KO 后判断 noBench → 输掉
      * 否则发起 promoteFromKO prompt 让对方晋升后场
      * Promote 之后自动结束本回合（autoEndTurn）
      * 非 KO 攻击直接 autoEndTurn（v0 简化：所有攻击都直接结束回合，
        因为状态异常 checkup 在 v0 内没真实效果）
  - 11 个新单测：基础伤害 / 弱点 / 能量不足拒绝 / 首回合禁攻 / KO 1-prize /
    ex KO 2-prize / 无后场判负 / promote 流程 / 6-prize 胜利 / 自动 endTurn /
    未知 action 不崩
  - 5 个 test suite，共 48 tests；init.sh 全绿

=== Session F6 | 2026-05-03 ===
Feature: 三套卡组的关键卡牌效果（priority 6）
Status: completed
Notes:
  - core/effects.ts 重构成 4 个注册表：Trainer / Attack / Ability / OnPlay + EffectStep
  - state.ts 新增 pendingEffect 字段（multi-step 效果状态机用）
  - actions.ts 新增 UseAbility action
  - reducer：
      * handleUseAbility 派发到 ABILITY_EFFECTS
      * handleAttack 调用 attack effect 计算最终伤害
      * handlePlayBasicPokemon 触发 onPlay
      * handleResolvePrompt 优先派发 pendingEffect 的 step 处理器
      * handleStartTurn 重置 abilityUsedThisTurn / playedThisTurn / evolvedThisTurn
  - effects/charizard-ex.ts：Burning Darkness 攻击效果（base 180 + 30/对手取奖励）
  - effects/miraidon-ex.ts：Tandem Unit 入场效果（自动搜 2 张基础雷 → 后场）
  - effects/pidgeot-ex.ts：Quick Search 能力（multi-step：发 prompt → 搜卡 → 入手）
  - effects/gardevoir-ex.ts：Psychic Embrace 能力（弃牌区超能 → 我方超系，+20 dmg）
  - effects/trainers.ts：Professor's Research / Iono / Boss's Orders / Switch / Nest Ball
  - effects/helpers.ts：drawN / shuffleDeck / shuffleHandIntoDeck / discardHand /
    moveDeckToHand / moveDeckToBench / findBasicInDeck 等公共 helper
  - 13 个新单测，全过；6 suites 61 tests；init.sh 全绿
  - 暂未实装：Flaaffy Dynamotor / Kirlia Refinement / Rare Candy / Ultra Ball /
    Arven / 其他次要 trainer。这些在 F14 之前如有时间可补；不影响 v0 demo 流程。

=== Session F7 | 2026-05-03 ===
Feature: AI 启发式对手（priority 7）
Status: completed
Notes:
  - core/ai/heuristics.ts：can-pay-cost / pickBestAttack / findEvolution /
    findUsableAbility 等纯函数 helper
  - core/ai/policy.ts：chooseAction 主入口 + simulateGame 仿真器
  - AI 优先级：
      1. 用能力（Pidgeot Quick Search / Gardevoir Psychic Embrace）
      2. 进化（hand 有进化 + 在场对应 Basic + 非首回合 + 不是同回合放出）
      3. 出 Basic 到后场（如果 hand 有 Basic + 后场有空槽）
      4. 出 Supporter（非首回合 + 每回合一次）
      5. 出 Nest Ball（如果 bench 有空）
      6. 挂能量到前场
      7. 攻击（最高伤害可负担的招式）
      8. EndTurn 兜底
  - 4 个新单测：3 套卡组对战不崩 + 30 局 mirror 不崩 + 9 个 cross-deck pairing
  - 7 suites 66 tests 全过；init.sh 全绿

=== Session F8 | 2026-05-03 ===
Feature: UI 骨架 + Tableau 布局（priority 8）
Status: completed
Notes:
  - app/src/ui/battle/Tableau.tsx：PTCG Live 风格布局
      * 顶部 PlayerSide(opp, mirrored=true)：bench (5 槽) + 前后场 + 牌组/弃牌/奖励
      * 中部 Stadium 接缝
      * 下部 PlayerSide(self)：对称布局
      * 最底部 HandStrip：玩家手牌横向滚动
  - app/src/ui/battle/GameCanvas.tsx：顶层容器 + 右上角 Sidebar（回合 / 阶段 / 事件日志）
  - app/src/ui/battle/BattlePage.tsx：客户端组件，调用 autoSetup 生成 mock 状态
  - app/src/app/battle/page.tsx：Next.js 服务端入口，从 ?deck= ?opp= ?seed= query 读 props
  - app/src/app/page.tsx：重写为 3 卡组选择器（点选直接跳到 /battle?deck=...）
  - 浏览器验证：1440×900 viewport 下，完整看到对手 + 中央 Stadium + 自己 + 手牌结构
    - 喷火龙 vs 密勒顿对战页：active 显示 Pidgey vs Iron Hands ex；
      奖励 6 张可视化、bench 5 槽、deck 47 / discard 0 计数正确；
      手牌横排显示具体卡名（Switch / Arven / Nest Ball / Professor's Research 等）
  - 浏览器 console 无错误；init.sh 全绿
  - F8 是「静态 mock state」阶段，无交互；F10+ 接入拖拽 + 行动 dispatch

=== Session F9 | 2026-05-03 ===
Feature: BoardCard / HandCard / EnergyChip 视觉到位（priority 9）
Status: completed
Notes:
  - ui/battle/cards/EnergyChip.tsx：9 种能量按属性着色（Fire 橙 / Water 蓝 /
    Grass 绿 / Lightning 黄 / Psychic 紫 / Fighting 棕 / Darkness 灰 / Metal 银 /
    Colorless 浅灰）+ 字母标识 + 1px ring + 阴影
  - ui/battle/cards/BoardCard.tsx：完整棋盘卡渲染
      * Next.js Image 加载 pokemontcg.io 卡图（unoptimized）
      * HP 进度条（顶部 1.5px，>50% 绿，>25% 黄，否则红）
      * 伤害指示器（右上角，红色徽章 -<damage>）
      * 能量徽章（底部横排）
      * 状态徽章（左上角）
      * ex/V/VMAX/VSTAR 全息渐变叠加层（mix-blend-mode: screen）
      * active 尺寸 112×156 + 紫色光晕 box-shadow，bench 尺寸 72×100
  - ui/battle/cards/HandCard.tsx：手牌小尺寸（68×96），hover 抬起 + 紫色光晕
  - Tableau.tsx 重写：用 BoardCard / HandCard 替换内联渲染；牌组叠层效果
    （多层 div 偏移 -0.5/-1 模拟一叠卡）；手牌 -16px margin 实现 fan 叠加
  - 浏览器验证（1024×700）：
      * 喷火龙 vs 密勒顿对战页：双方 active 卡图正确加载（Iron Hands ex / Pidgey），
        opp bench 显示 Mareep 卡图，6 张奖励叠层可视，手牌 6 张全部加载图
        （Switch / Arven / Nest Ball ×2 / Professor's Research / Basic Fire Energy）
      * 浏览器 console 无错误
  - init.sh 全绿；7 suites 66 tests 仍全过

=== Session F10+F11+F12 | 2026-05-03 ===
Feature: 完整交互层 + Prompt 渲染（priority 10-12 一并完成）
Status: completed
Notes:
  - useGame.ts：useReducer 包裹引擎 reducer + AI useEffect 驱动器
      （监听 state，AI 回合或 AI 收到 prompt 时延迟 350ms 自动派发）
  - CardActionMenu.tsx：点击卡片弹出操作菜单（替代拖拽）
      * 手牌 Basic：放后场
      * 手牌 进化：进化（首回合禁止 + 同回合禁止）
      * 手牌 能量：挂到前/后场任一目标
      * 手牌 训练家：使用（按 Item / Supporter / Stadium 区分，含每回合 / 首回合限制）
      * 前场：所有招式（含能量校验）+ 能力 + 撤退（自动选首张能量付费）
      * 后场：能力（如 Pidgeot Quick Search）
  - PromptStack.tsx：8 种 prompt kind 全部 UI 渲染
      * selectActiveSetup / selectBenchSetup / promoteFromKO（基础设施类）
      * selectFromList / selectTarget（卡牌效果类）
      * coinFlip / confirm / chooseOption（一般 prompt）
      * 自动只渲染 humanPlayer 的 prompt（AI 的 prompt 由 useGame useEffect 处理）
  - GameCanvas.tsx 重写：含 Sidebar（结束回合按钮、AI thinking 状态、log）+ GameOverOverlay
  - Tableau.tsx 重写：所有 zone 接受 onCardClick，前场/后场/手牌都可点击
  - BattlePage.tsx：起手「抽牌」按钮触发 StartTurn（事件总线方式连到 sidebar）
  - 浏览器验证（1280×800）：
      * 加载 /battle?deck=charizard-ex&opp=miraidon-ex&seed=99
      * 「抽牌」按钮派发 StartTurn → 阶段 main / 抽 1 张（46 deck，6 hand）
      * 点击手牌 Charmander → 弹出操作菜单显示「放到后场」+ 卡牌详情
      * Battle Log 显示完整事件链
      * 浏览器 console 无错误
  - init.sh 全绿；7 suites 66 tests 仍全过
  - F10/F11/F12 一并完成（user 要求全自动模式连续推进）
  - 暂未实装 dnd-kit drag（用 click 代替）；F13 加 framer-motion 动画

=== Session F13 | 2026-05-03 ===
Feature: 动画 + 特效层（priority 13）
Status: completed
Notes:
  - BoardCard / HandCard 包 motion.div：
      * layoutId（FLIP 自动布局动画 — 卡从手牌飞到场上时会插值）
      * initial / animate / exit 配合 AnimatePresence
      * spring transition (stiffness 220-280, damping 24-28)
  - HandCard 加 whileHover：y -8, scale 1.1（替代纯 CSS hover）
  - 新建 fx/DamageFlash.tsx：监听 log 中最近的 Attack 事件，
    在屏幕中央闪现红色伤害数字（-XX），1.2s 动画（fade + scale + 上升）
  - GameCanvas 挂载 DamageFlash
  - 浏览器验证：页面加载无 console error；motion.div 节点正常工作
  - init.sh 全绿
  - v0 范围内的动画：卡牌进出场 + 攻击伤害闪烁。
    更详细的 KO 烟雾 / 取奖励飞行 cinematic 等留待 v0.2+

=== Session F14 | 2026-05-03 ===
Feature: 三套卡组 E2E 烟雾测试 + v0.1 收尾（priority 14）
Status: completed
Notes:
  - 新建 core/__tests__/e2e.test.ts：
      * 27 个跨卡组对战 case（3 卡组 × 3 卡组 × 3 seeds）
        全部通过 — 没有崩溃，迭代数 > 10
      * 引擎事件 log 完整性验证（GameStart / PlaceActiveSetup /
        PlaceBenchSetup / StartTurn 全部记录）
  - 8 suites 94 tests 全过；init.sh 全绿
  - README 重写当前状态节为「v0.1 已完成」+ 功能清单
  - feature_list.json：15/15 priority 全部 passes:true
  - 准备打 git tag v0.1
