=== Session F0 (tabula rasa) | 2026-05-03 ===
Feature: 清场 + 重建骨架（priority 0）
Status: completed
Notes:
  - 用户决策：完全重置 (tabula rasa)，从三套官方大师卡组（喷火龙 ex / 密勒顿 ex /
    沙奈朵 ex）+ 单人 PvE + 桌面浏览器 + PTCG Live 风格 UI 起步。
  - 已把以下旧资产移到 archive/legacy-2026-05-03/（git mv 跟踪历史）：
      * 根目录：spec.txt / feature_list.json / claude-progress.txt / plan.md /
        Poke-Trainer-Pro_Agent-Teams设计方案.md / init.sh / card test/ /
        files (1).zip
      * app/：src/ / server.ts / tsconfig.server.json / PROJECT_ARCHITECTURE.md /
        scripts/ / docs/
  - 已修补：
      * app/package.json：dev 脚本改为 next dev，删除 generate-rules
      * app/tsconfig.json：删除 @ptcg/common paths 与遗留 exclude
      * app/jest.config.ts：删除 @ptcg/common moduleNameMapper 与 ptcg-engine 忽略
      * app/next.config.ts：删除 /battle/page 的旧 redirect 与 scrydex.com 域名
  - 已新建：
      * app/src/app/layout.tsx / page.tsx / globals.css
      * app/src/app/api/health/route.ts（GET 返回 status=ok）
      * feature_list.json（15 条 v0 feature，priority 0-14）
      * spec.txt（v0 精简规格 ~100 行）
      * init.sh（启动 dev / 健康检查 / 跑测试 / 收尾）
  - 引擎、UI、卡牌数据全部为零；F1 起逐 feature 接入。
  - 开放问题待用户答复（详见 plan 文件「开放问题」节）：
      1. 三套大师卡组的官方卡表来源
      2. 卡图获取方式（pokemontcg.io 自动拉取 vs 用户提供官方中文版）
      3. AI 难度级别
      4. v0 完结后是 PvP 还是扩卡
      5. 音效是否预留挂载点

=== Session F1 | 2026-05-03 ===
Feature: 卡牌数据骨架 + 喷火龙 ex 大师卡组（priority 1）
Status: completed
Notes:
  - 新建 app/src/core/types.ts：CardDef union（Pokemon / Trainer / Energy）+ DeckDef + 9 种 EnergyType
  - 新建 app/src/core/cards.ts：Map registry + registerCard / getCard / validateDeck
  - 新建 app/src/core/decks/charizard-ex.ts：17 张 unique CardDef + DeckDef（slug=charizard-ex）
  - 卡图通过 pokemontcg.io 远程 URL（next.config.ts 已配 remotePatterns）
  - 8 个单元测试全过；init.sh 全绿
  - 卡表参考归档版 archive/.../prebuilt-decks.ts；标准 SV 时代 Charizard ex / Pidgeot ex 引擎主线，
    与中国官方大师卡组的具体卡数可能略有出入，待用户提供官方卡表后再 reconcile。

=== Session F2 | 2026-05-03 ===
Feature: 密勒顿 ex + 沙奈朵 ex 大师卡组（priority 2）
Status: completed
Notes:
  - 新建 app/src/core/decks/miraidon-ex.ts：10 张 unique CardDef + 60 张 deck list
  - 新建 app/src/core/decks/gardevoir-ex.ts：10 张 unique CardDef + 60 张 deck list
  - 修改 app/src/core/decks.ts：DECKS 暴露 3 套
  - 13 个单测全过；init.sh 全绿
  - 偏离归档版地方：
      Miraidon 把 2 Regieleki VMAX 替换成 2 Iron Hands ex (PAR 70)，
      避免 VMAX 没有对应 V 的非法 deck 构造（vMAX 必须有同名 V）。
      Iron Hands ex 是真实的 SV 时代 Miraidon 顶级搭档，Standard 合法。
  - 三套卡组共 37 张 unique 卡牌（Charizard 17 + Miraidon 复用 + Gardevoir 复用 + 新增）

=== Session F3 | 2026-05-03 ===
Feature: 游戏状态 + 阶段机 + 起手抽牌（priority 3）
Status: completed
Notes:
  - core/state.ts：GameState / PlayerState / GameCard / GamePhase / Prompt / StatusCondition；
    BENCH_SIZE=5 / HAND_DRAW=7 / PRIZE_COUNT=6
  - core/actions.ts：Action union（GameStart / PlaceActiveSetup / PlaceBenchSetup /
    DrawCard / ResolvePrompt / Concede）+ PromptResponse
  - core/rng.ts：mulberry32 seedable RNG + Fisher-Yates shuffle
  - core/reducer.ts：完整 setup 流程 (GameStart → mulligan + draw 7 → prompt
    selectActive → prompt selectBench → 切换玩家 → 翻 6 prizes → phase=draw)
  - autoSetup() helper：自动响应 setup 提示，用于测试和 AI（F7 会复用）
  - 12 个新单测全过；init.sh 全绿
  - Mulligan：手牌无 Basic 时 reshuffle 重抽（不实现「对手多抽 1 张」补偿；F4+ 再补）
