# ── Poke-Trainer Pro Environment Variables ──

# Server
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# WebSocket URL (used by the client to connect to the game server)
# In production, set this to your public domain/IP
NEXT_PUBLIC_SOCKET_URL=http://localhost:3000
